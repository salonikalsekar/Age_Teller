# Age_Teller
This is a basic ReactJS application.


It helps us to determine the age using the birthdate of the user.

To run the project you need to install all the packages using npm install 
To implement the application using npm start
